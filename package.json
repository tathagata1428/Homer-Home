{
  "name": "homer-home",
  "version": "2.2.2",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node scripts/validate-static.mjs",
    "build": "npm run check",
    "deploy": "npx wrangler pages deploy ."
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.4"
  },
  "devDependencies": {
    "pg": "^8.20.0",
    "wrangler": "file:./tools/wrangler"
  }
}
