package ro.b4it.homer.ui.theme

import androidx.compose.ui.graphics.Color

// Homer — warm paper journal · Notion × Day One

// ── Surfaces ──────────────────────────────────────────────────────────────────
val BgPrimary      = Color(0xFFF5F0E8)   // warm parchment page
val BgCard         = Color(0xFFFEFCF8)   // near-white card surface
val BgCardAlt      = Color(0xFFEEE8DC)   // secondary surface / nested
val BgInput        = Color(0xFFF7F2EA)   // input field background
val BgHover        = Color(0xFFE8E0D0)   // hover / pressed state

// ── Text ──────────────────────────────────────────────────────────────────────
val TextPrimary    = Color(0xFF1A1614)   // near-black charcoal (warm)
val TextMuted      = Color(0xFF6B5E52)   // warm medium brown
val TextSubtle     = Color(0xFFA8998A)   // soft warm gray

// ── Accents ───────────────────────────────────────────────────────────────────
val AccentBlue     = Color(0xFF2860C8)   // deep ink blue
val AccentBlueLight= Color(0xFF5A8ED8)   // lighter ink blue
val AccentViolet   = Color(0xFF7054B4)   // muted violet
val AccentGreen    = Color(0xFF2D7A56)   // forest green
val AccentAmber    = Color(0xFFA87020)   // warm amber
val AccentOrange   = Color(0xFFA85820)   // burnt sienna
val AccentRed      = Color(0xFFA82828)   // deep crimson
val AccentCyan     = Color(0xFF1A6E80)   // teal

// ── Semantic aliases — backward compat with all screens ───────────────────────
val NeonPink       = Color(0xFFB84060)   // muted berry rose
val NeonCyan       = Color(0xFF1A6E80)   // teal ink
val NeonPurple     = Color(0xFF7054B4)   // muted violet
val NeonGold       = Color(0xFFA87020)   // warm amber

// ── Borders ───────────────────────────────────────────────────────────────────
val BorderDefault  = Color(0x14000000)   // black 8 %
val BorderSubtle   = Color(0x0A000000)   // black 4 %
val BorderAccent   = Color(0x28000000)   // black 16 %
