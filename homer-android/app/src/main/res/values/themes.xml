<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme; Compose takes over from here. Dark-only. -->
    <style name="Theme.Homer" parent="android:Theme.Material.NoTitleBar">
        <item name="android:windowBackground">@color/bg_primary</item>
        <item name="android:statusBarColor">@color/bg_primary</item>
        <item name="android:navigationBarColor">@color/bg_primary</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Splash screen theme (shown before Compose loads) -->
    <style name="Theme.Homer.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/bg_primary</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_homer_splash</item>
        <item name="postSplashScreenTheme">@style/Theme.Homer</item>
    </style>
</resources>
